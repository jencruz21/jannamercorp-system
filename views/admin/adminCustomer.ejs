<%- include('include/header') %>
 
<div class="card">
    <div class="card-header">
        <h5>Customer List</h5>
        <!-- <span>use class <code>table-dark</code> inside table element</span> -->
        <div class="card-header-right">
            <ul class="list-unstyled card-option">
                <li><i class="fa fa fa-wrench open-card-option"></i></li>
                <li><i class="fa fa-window-maximize full-card"></i></li>
                <li><i class="fa fa-minus minimize-card"></i></li>
                <li><i class="fa fa-refresh reload-card"></i></li>
                <li><i class="fa fa-trash close-card"></i></li>
            </ul>
        </div>
    </div>
    <div class="card-block table-border-style" style="overflow-y:scroll;height:500px;">
        <div class="table-responsive">
            <table class="table table-dark">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Address</th>
                        <th>Contact #</th>
                        <th>Date</th>
                        <th>Credit Limit</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(var i = 0; i < customers.length; i++) { %>
                    <tr>
                        <th scope="row"><%= i + 1 %></th>
                        <td><%= customers[i].custName %></td>
                        <td><%= customers[i].custLocation %></td>
                        <td><%= customers[i].custPhone %></td>
                        <td><%= customers[i].custDate %></td>
                        <td><%= customers[i].custCreditLimit %></td>
                    </tr>
              <% } %>
                 
                </tbody>
            </table>
        </div>
    </div>
</div>

<%- include('include/footer') %>