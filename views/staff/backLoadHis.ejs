<%- include('include/header') %>

<div class="card">
    <div class="card-header">
        <h5>Backload History</h5>
        <!-- <span>use class <code>table-dark</code> inside table element</span> -->
        <div class="card-header-right">
            <ul class="list-unstyled card-option">
                <li><i class="fa fa fa-wrench open-card-option"></i></li>
                <li><i class="fa fa-window-maximize full-card"></i></li>
                <li><i class="fa fa-minus minimize-card"></i></li>
                <li><i class="fa fa-refresh reload-card"></i></li>
                <li><i class="fa fa-trash close-card"></i></li>
            </ul>
        </div>
    </div>
    <div class="card-block table-border-style" style="overflow-y:scroll;height:500px;">
        <div class="table-responsive">
            <table class="table table-dark">
               
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Date Time</th>
                        <th>Item</th>
                        <th>Type</th>
                    
                        <th>Sacks</th>
                        <th>Remarks</th>
                        <th>Encode By</th>
                        <th>Rice Mill</th>
                     

                  
                    </tr>
                   
                </thead>
                <tbody>
               
                    <% for(var i = 0; i <backLoad.length; i++) { %>
                 
                    <tr>
                        <th><%= i + 1 %></th>
                        <td><%=backLoad[i].dateTime %></td>
                        <td><%=backLoad[i].item %></td>
                        <td><%=backLoad[i].type %></td>
                        <td><%=backLoad[i].sacks %></td>
                        <td><%=backLoad[i].remarks %></td>
                        <td><%=backLoad[i].encodeBy %></td>
                        <td><%=backLoad[i].riceMill %></td>
                        
                       <td>
                      
                            <span class="text-gradient"><i class="fas fa-times" style="color: rgb(245, 5, 5);"></i></span>
                        </a>
                       </td>

                    </tr>
                    <% } %>
               
            
                 
                </tbody>
            </table>
        </div>
    </div>
</div>


<%- include('include/footer') %>
